interface Props {
  color?: string;
  classes?: string[];
}

const Plus = ({ color = "#2C2C54", classes = ["w-5 h-5"] }: Props) => {
  return (
    <svg
      xmlns="http://www.w3.org/2000/svg"
      fill="none"
      viewBox="0 0 24 24"
      strokeWidth={2}
      stroke={color}
      className={classes?.join("")}
    >
      <path
        strokeLinecap="round"
        strokeLinejoin="round"
        d="M12 4.5v15m7.5-7.5h-15"
      />
    </svg>
  );
};

export default Plus;
